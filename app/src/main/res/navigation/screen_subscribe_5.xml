<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/screen_subscribe_2">

    <fragment
        android:id="@+id/examination_health_result_detail_fragment"
        android:name="pet.perpet.equal.presentation.supplement.fragment.SupplementPackageDetailFragment">

        <argument
            android:name="medical"
            android:defaultValue="@null"
            app:argType="pet.perpet.domain.model.medical.Medical"
            app:nullable="true" />

        <argument
            android:name="pet_id"
            android:defaultValue="@null"
            app:argType="string"
            app:nullable="true" />

        <argument
            android:name="name"
            android:defaultValue="@null"
            app:argType="string"
            app:nullable="true" />


        <argument
            android:name="result"
            android:defaultValue="false"
            app:argType="boolean" />

    </fragment>

    <fragment
        android:id="@+id/supplement_package_fragment"
        android:name="pet.perpet.equal.presentation.supplement.fragment.SupplementPackageFragment">

        <argument
            android:name="medical"
            android:defaultValue="@null"
            app:argType="pet.perpet.domain.model.medical.Medical"
            app:nullable="true" />

        <argument
            android:name="pet_id"
            android:defaultValue="@null"
            app:argType="string"
            app:nullable="true" />

        <argument
            android:name="name"
            android:defaultValue="@null"
            app:argType="string"
            app:nullable="true" />


        <argument
            android:name="result"
            android:defaultValue="false"
            app:argType="boolean" />

    </fragment>


    <fragment
        android:id="@+id/supplement_package_detail_code_fragment"
        android:name="pet.perpet.equal.presentation.supplement.fragment.SupplementPackageDetailCodeFragment">

        <argument
            android:name="medical"
            android:defaultValue="@null"
            app:argType="pet.perpet.domain.model.medical.Medical"
            app:nullable="true" />

        <argument
            android:name="pet_id"
            android:defaultValue="@null"
            app:argType="string"
            app:nullable="true" />

        <argument
            android:name="name"
            android:defaultValue="@null"
            app:argType="string"
            app:nullable="true" />

        <argument
            android:name="result"
            android:defaultValue="false"
            app:argType="boolean" />

    </fragment>




</navigation>
